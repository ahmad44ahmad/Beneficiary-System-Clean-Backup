# 🛠️ دليل إعداد قاعدة البيانات (Database Setup Guide)

هذا الدليل يشرح بالتفصيل كيفية إعداد قاعدة بيانات Supabase وربطها بمشروعك.

## 1. إنشاء مشروع Supabase

1. اذهب إلى [https://supabase.com](https://supabase.com) وقم بتسجيل الدخول.
2. اضغط على **"New Project"**.
3. اختر المنظمة (Organization).
4. أدخل اسم المشروع (مثلاً: `beneficiary-management`).
5. أدخل كلمة مرور قوية لقاعدة البيانات (احفظها جيداً!).
6. اختر المنطقة (Region) الأقرب إليك (مثلاً: Frankfurt أو London).
7. اضغط **"Create new project"**.

## 2. إعداد هيكل قاعدة البيانات (Schema)

1. بعد إنشاء المشروع، اذهب إلى القائمة الجانبية واختر **"SQL Editor"**.
2. اضغط على **"New query"**.
3. انسخ محتوى الملف `outputs/database/schema.sql` بالكامل.
4. الصق المحتوى في المحرر.
5. اضغط على زر **"Run"** (أو `Ctrl + Enter`).
6. تأكد من ظهور رسالة "Success" في النتائج.

## 3. الحصول على مفاتيح API

1. اذهب إلى **Project Settings** (رمز الترس في الأسفل).
2. اختر **"API"**.
3. ستجد قسم **Project URL** و **Project API keys**.
4. ستحتاج إلى:
   - **URL**: رابط المشروع.
   - **anon public**: المفتاح العام (للاستخدام في التطبيق).

## 4. إعداد متغيرات البيئة

1. في مجلد مشروعك الرئيسي، قم بإنشاء ملف باسم `.env.local` (إذا لم يكن موجوداً).
2. انسخ محتوى `outputs/.env.example` إلى `.env.local`.
3. استبدل القيم بالقيم التي حصلت عليها في الخطوة السابقة:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

## 5. دمج الملفات في المشروع

قم بنقل الملفات من مجلد `outputs` إلى مجلد `src` في مشروعك:

1. انسخ `outputs/src/config/supabase.ts` إلى `src/config/supabase.ts`.
2. انسخ مجلد `outputs/src/api` إلى `src/api`.
3. انسخ مجلد `outputs/src/hooks` إلى `src/hooks`.

## 6. تثبيت المكتبات المطلوبة

تأكد من تثبيت مكتبة Supabase في مشروعك:

```bash
npm install @supabase/supabase-js
```

## 7. التحقق من الاتصال

يمكنك استخدام المكون `BeneficiaryManagementExample.tsx` للتحقق من أن كل شيء يعمل بشكل صحيح.

1. انسخ `outputs/src/components/examples/BeneficiaryManagementExample.tsx` إلى مشروعك.
2. قم باستيراده في إحدى الصفحات (مثلاً `App.tsx` أو صفحة جديدة).
3. شغل المشروع: `npm run dev`.
4. إذا ظهرت قائمة المستفيدين (فارغة في البداية) وبدون أخطاء في Console، فالإعداد ناجح!

## 8. ملاحظات أمنية

- **Row Level Security (RLS)**: تم تفعيل RLS في جميع الجداول. هذا يعني أنه افتراضياً، لن يتمكن أحد من قراءة أو كتابة البيانات إلا إذا أضفت سياسات (Policies).
- في `schema.sql`، قمنا بإضافة سياسات بسيطة تسمح للمستخدمين المسجلين (Authenticated) بالقراءة.
- يجب عليك مراجعة وتعديل هذه السياسات قبل النشر الفعلي لتناسب متطلبات الأمان الخاصة بك (مثلاً: الطبيب يرى الملفات الطبية فقط).

## 🆘 هل تحتاج مساعدة؟

إذا واجهت أخطاء، تأكد من:
1. أنك نسخت المفاتيح الصحيحة (anon key وليس service_role key).
2. أنك نفذت كود SQL بالكامل.
3. راجع تبويب **Table Editor** في Supabase للتأكد من إنشاء الجداول.
